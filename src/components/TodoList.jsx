import React from 'react';
// import styles from './TodoList.less';

const TodoList = () => {
    return (
        <div>
            TodoList
        </div>
    );
};

TodoList.propTypes = {};

export default TodoList;
